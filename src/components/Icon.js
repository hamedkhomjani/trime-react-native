import React from 'react'
import SvgIcon from 'react-native-svg-icon'
import svgs from '../../assets/svgs.js'

const Icon = props => <SvgIcon {...props} strokeWidth={1} svgs={svgs} />

export default Icon
